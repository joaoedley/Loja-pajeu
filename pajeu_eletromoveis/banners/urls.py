from django.urls import path
from . import views

app_name = 'banners'

urlpatterns = [
    # URLs para API (se necessário)
    # path('api/banners/', views.BannerListAPIView.as_view(), name='banner-list'),
]